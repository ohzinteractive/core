import type { Material } from "three";
import { Mesh } from "three";
export class VerticalPlane extends Mesh {
    constructor(width?: number, height?: number, color?: number | string, material?: Material);
}
